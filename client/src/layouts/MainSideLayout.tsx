import styled from "styled-components";

const MainSideLayout = styled.div`
  display: grid;
  grid-template-columns: minmax(0, 17fr) minmax(0, 7fr);
  gap: 1.2rem;
`;

export default MainSideLayout;
