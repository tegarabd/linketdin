import styled from "styled-components";

const SideMainAsideLayout = styled.div`
  display: grid;
  grid-template-columns: minmax(0, 5fr) minmax(0, 12fr) minmax(300px, 7fr);
  gap: 1.2rem;
`;

export default SideMainAsideLayout;
